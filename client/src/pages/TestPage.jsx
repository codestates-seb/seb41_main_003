import { AlertModal } from '../components/modal/DefaultModal';

const TestPage = () => {
  return (
    <div>
      여기는 테스트 페이지인데용?
      <AlertModal text="alertModal 만드는 중" />
    </div>
  );
};

export default TestPage;
